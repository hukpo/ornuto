export type LanguageCode = 'en' | 'ua';

export type LanguageFiles = {
  common: number;
  navigation: number;
  settings: number;
  boxes: number;
  chat: number;
  gallery: number;
  auth: number;
};
